# Figures for results_phase0_5.md

Drop the notebook's saved PNGs here (they're written to the Drive `ART` dir each run):

- `g3_operand_curve.png`  — Phase 3 must-compute curve (accuracy vs operand size)
- `p35_controls.png`      — Phase 3.5 control battery (exact accuracy by condition C0–C7)
- `g4_patch_heatmap.png`  — Phase 5 / G4 addition activation-patching recovery heatmap

`results_phase0_5.md` references them at `figures/<name>.png`.
